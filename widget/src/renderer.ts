/**
 * DEPRECATED — This file is no longer the renderer entry point.
 *
 * The renderer UI lives in src/renderer/renderer.ts and is bundled by
 * esbuild (see package.json "bundle:renderer" script) into dist/renderer.js.
 *
 * This file is kept only to avoid breaking tsc compilation of the existing
 * project structure. It produces no meaningful output.
 */
export {};
